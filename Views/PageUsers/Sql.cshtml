@model IEnumerable<TrojWebApp.Models.PageUsers3Model>
@{
    ViewData["Title"] = "Insert into PageUsers3";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>@ViewData["Title"]</h1>
<table class="table table-condensed table-no-border">
    @foreach (var item in Model)
    {
        <tr>
            <td class="subhead2" nowrap>
                INSERT INTO PageUsers3 (PageId,EmployeeId,Changed,ChangedBy)
                VALUES(
                @item.PageId,
                @item.EmployeeId,
                '@item.Changed',
                '@item.ChangedBy'
                );
            </td>
        </tr>
    }
</table>
